name: Static Terraform Checks

on:
  push:
    branches:
      - '**'

jobs:
  static-analysis:
    name: Terraform Static Analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra/tf-app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          enable-AzPSSession: false  # ✅ Required for Terraform to receive ARM_ env vars

      - name: Deb
